import java.util.Scanner;

public class Num {
    Scanner sc = new Scanner(System.in);
    int inputNum;

    public Num() {
        inputNum = sc.nextInt();
    }
}
// Integer 클래스처럼 원시 자료형을 굳이 쓰지 않아도 되는 방법이 있다.
// 다양한 메서드와 몇 가지 멤버변수가 제공되므로

// 지금 입력 + 저장 이란 기능이 섞여 있다
// InputHandler 같은 클래스를 만들어보자

// 자료구조를 쓰는 아니라 ArrayList
// BigDecimal를 사용하면 큰 값을 반영할 수 있다.

